# OpenConnect VPN configuration
# Copy to .env and fill in your credentials

# VPN portal hostname (no https://)
OC_PORTAL=vpn.example.com

# VPN username
OC_USER=your_username

# Protocol: gp (GlobalProtect), anyconnect, fortinet, f5, pulse, nc, array
OC_PROTOCOL=gp
